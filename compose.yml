services:

  tox:
    image: makukha/multipython:unsafe
    working_dir: /work
    entrypoint: tox
    volumes:
      - virtualenv_app_data:/root/.local/share/virtualenv
      - .:/work

volumes:
  virtualenv_app_data:
